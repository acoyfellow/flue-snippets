---
title: d1
tagline: 'SQLite at the edge. INSERT a row, SELECT it back.'
composes: [D1]
---

# d1

> SQLite at the edge. INSERT a row, SELECT it back.

```sh
bash examples/d1/run-e2e.sh
```

`env.DB.prepare(sql).bind(...).run()` then SELECT to verify.
